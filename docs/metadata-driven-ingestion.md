# Metadata-driven ingestion

Metadata-driven ingestion defines entities, source/target mapping, load strategy, and dependencies in configuration files.

Benefits:
- standardization
- easier review and automation
- earlier detection of config defects

This project validates core guardrails on synthetic metadata before deployment.
