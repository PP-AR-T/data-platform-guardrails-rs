# Integration test scenarios

This directory stores end-to-end and environment-coupled scenarios.

- `file-fixtures/`: source metadata loaded from local files.
- `sqlserver/`: dedicated SQL Server integration scenarios (optional and not part of default CI quick path).

Place heavy integration suites here and execute conditionally in CI.
